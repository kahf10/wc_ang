# macOS
.DS_Store

# Editor crud
.vscode/
.idea/
*.swp

# Excel temp files
~$*.xlsx
~$*.docx

# Scoring spreadsheet stays local + Google Drive only, never published in repo
*.xlsx

# Internal docs — kept on commissioner machine only
WC_ANG_TRD.md
WC_ANG_TRD.docx
COMMISSIONER.md

# Local helper file for manually setting up the Recent_Results_CSV tab
recent_results_paste.tsv

# Python
__pycache__/
*.pyc

# Claude Code local session/config — not for sharing in the repo
.claude/

# Working-copy spreadsheet exports / extracted data — never commit raw data dumps
*.csv
wc_ang_DagiUnited (Working Copy)*
